import 'package:flutter/material.dart';

class Quarter extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(
          child: Icon(Icons.photo_album, size: 150.0, color: Colors.pink)
      ),
    );
  }
}